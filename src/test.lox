class Animal {
    init(name) {
        this.name = name;
    }

    run() {
        print f "the Animal {this.name} is running!";
    }
}

print Animal;

class Dog: Animal {
    init(name) {
        this.name = name;
    }
    run() {
        //super.run();
         print f "the Dog {this.name} is running!";
    }
}

class BigDog: Dog {
    init(name) {
        super.init(name);
    }
}

var bd = BigDog("anda");

bd.run();
